import CreativitySection from "../components/CreativitySection";
import CoffeeColorCards from "../components/CoffeeColorCards";
// CoffeeStory import removed — it's not being used

const StoryPage = () => {
  return (
    <div className="w-full">
      <CreativitySection />
      <CoffeeColorCards />
    </div>
  );
};

export default StoryPage;
