# Optional: base URL for your Node backend (no trailing slash required).
# When unset, checkout uses a fake API (delay + localStorage key coffeeShopPlacedOrders).
# VITE_API_URL=http://localhost:3000
